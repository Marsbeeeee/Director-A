# Task XX: Name

- Status: Planned
- Owner: Product / Frontend / Backend
- Last Updated: YYYY-MM-DD
- Source Of Truth: 本文档为 Task XX 当前交付说明
- Depends On:
- Related Docs:

## 目标

说明这期要解决的核心问题，只写 1 到 3 句。

## 本期范围

1. 写清本期必须交付的能力。
2. 每条都应该对应可实现或可验证的结果。
3. 避免使用只有方向没有行为的表述。

## 非目标

1. 写清这期明确不做什么。
2. 写清暂不优化什么。
3. 写清哪些延后到后续 task。

## 关键场景

1. 场景一：谁在什么上下文触发了这个功能，系统应返回什么。
2. 场景二：输入不完整、低置信或失败时，系统应如何处理。
3. 场景三：需要保存、跳转、重试或继续后续动作时，系统应如何衔接。

## 输入 / 输出

### 输入

- 用户输入：
- 上游模块输入：
- 必填字段：
- 可选字段：

### 输出

- 前端展示输出：
- 后端接口输出：
- 持久化输出：

## 状态与异常

- 成功态：
- 低置信态：
- 空结果态：
- 失败态：
- 权限或准入限制：

## 数据 / 接口 / UI 约束

- 依赖的数据契约：
- 涉及的 API：
- 是否允许改 schema：
- 是否允许改接口：
- 是否要求兼容现有前端行为：
- 是否需要移动端适配：

## 前端规范补充

若本任务涉及前端，必须明确写出以下内容：

- 页面归属：影响哪个页面，例如 `Workspace`、`Explorer`、`Profile`、管理员控制台等。
- UI 变更点：具体改动输入区、结果区、详情区、动作区、弹层还是导航。
- 前端消费规则：前端依赖哪些结构字段，哪些语义不能由前端自行猜测。
- 页面文档同步：需要同步更新哪些 `docs/frontend/pages/*.md`。

若本任务不涉及用户前台改动，也必须显式写：

- 本期无用户前台改动。
- 若未来需要内部工具 UI，应落到哪个内部页面或控制台。

推荐写法：

1. 页面归属：
2. UI 变更点：
3. 前端消费规则：
4. 页面文档同步：

## 落地范围

- 需要修改的模块：
- 可以新增的文件类型：
- 本期不应改动的模块：
- 测试要求：

## 验收

1. 用用户可感知结果写验收，不要只写技术动作。
2. 至少覆盖主路径。
3. 至少覆盖一个异常或边界路径。

## 验收示例

1. 当用户输入 `麦当劳双层吉士汉堡` 时，系统返回统一 `decision_card` 并展示推荐结论、风险和建议。
2. 当品牌置信度不足时，系统进入澄清态，而不是直接保存到 food log。
3. 当用户点击保存时，只允许满足保存条件的卡片进入 food log，并显示明确反馈。

## 相关文档

- `docs/rules/...`
- `docs/modules/...`
- `docs/system/...`
- `docs/product/...`
